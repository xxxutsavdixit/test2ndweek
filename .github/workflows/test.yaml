name: This is workflow
on:
     workflow_dispatch:
         inputs:
            color:
                description: "Please select the color"
                type: choice
                required: true
                options:
                    - red
                    - blue
                    - green

jobs:
    build:
     runs-on: ubuntu-latest
     steps:
      - name: print the color name
        run: |
                echo "Print the color ${{github.event.inputs.color}}"





